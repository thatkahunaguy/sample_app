<% if @feed_items.any? %>
  <ol class="microposts">
    <%# with a collection parameter partial syntax must be specified %>
    <%# the collection renders for each item in the collection %>
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>